// @curio/core — Curio's portable click-to-explain engine.
//
// Populated in phase 2 (pure engine: catalog, Ollama client, lookup, tokenize) and phase 3
// (the decoupled reading UI). For now this is the package entry stub so the workspace resolves.
export {};
